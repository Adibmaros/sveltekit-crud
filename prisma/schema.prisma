generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Mahasiswa {
  id Int @id @default(autoincrement())
  name String
  jurusan String
}
